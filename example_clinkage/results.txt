# to recreate run:

rm -r tenHHRing_CODE
rm generator
rm generator.d
rm Makefile
rm output.V.dat
rm output.spikes.dat 
rm stimulator
genn-buildmodel.sh -c model.cc;

# then move the closing "}" of the "extern "C"" scope in definitions.h to above "// synapse variables"
# then run
genn-create-user-project.sh example simulation.cc
make

# this removes the warnings for the functions that return a reference :
# warning: 'getPop1CurrentSpikeCount' has C-linkage specified, but returns user-defined type 'unsigned int &' which is incompatible with C [-Wreturn-type-c-linkage]
# EXPORT_FUNC unsigned int& getPop1CurrentSpikeCount(unsigned int batch = 0); 